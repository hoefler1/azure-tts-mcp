# Copy to .env only if you run the server standalone for testing.
# When used via Claude Desktop these are set in the MCP config "env" block instead.

AZURE_SPEECH_KEY=your-azure-speech-key
AZURE_SPEECH_REGION=westeurope
# Optional: where audio files are written (defaults to ./output)
# TTS_OUTPUT_DIR=/absolute/path/to/output
